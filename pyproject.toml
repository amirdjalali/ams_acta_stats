[project]
name = "ams-acta-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.6",
    "pymongo>=4.18.1",
]
